---
import type { BaseProps } from '../types'

interface Props extends BaseProps {}

const { class: className, ...rest } = Astro.props
---

<div
  class:list={['mx-auto w-full max-w-2xl px-6 md:px-8', className]}
  {...rest}
>
  <slot />
</div>
