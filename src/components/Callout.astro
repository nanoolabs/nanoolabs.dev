---
interface Props {
  type?: "default" | "info" | "warning" | "error";
}
const { type = "default" } = Astro.props;
let emoji = "💡";
if (type === "info") {
  emoji = "ℹ️";
} else if (type === "warning") {
  emoji = "⚠️";
} else if (type === "error") {
  emoji = "🚨";
}

const baseClasses = "relative my-4 flex rounded border p-3";
const typeClasses = {
  default: "border-orange-200/20 bg-orange-950/20 text-orange-200",
  info: "border-blue-200/20 bg-blue-950/20 text-blue-200",
  warning: "border-yellow-200/20 bg-yellow-950/20 text-yellow-200",
  error: "border-red-200/20 bg-red-950/20 text-red-200",
};
---

<div class:list={["not-prose", baseClasses, typeClasses[type]]}>
  <span class="pointer-events-none pr-3 text-xl select-none">{emoji}</span>
  <slot />
</div>
